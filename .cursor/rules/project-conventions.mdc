---
description: 本项目全局开发约定与规范
alwaysApply: true
---

# 项目全局约定

本规则适用于整个项目，所有页面与功能开发请遵循以下约定。

## UI 与组件

- **统一使用 [Wot UI](https://wot-ui.cn/)（wot-design-uni）** 作为 UI 组件库，不混用微信小程序原生自定义组件做 UI。
- 组件已通过 `@uni-helper/vite-plugin-uni-components` + `WotResolver` 按需引入，直接使用 `<wd-*>` 即可，无需手动 import。
- 组件文档与 API 以官网为准：<https://wot-ui.cn/>。

## 布局与底部导航

- 主包页面统一使用 **MainLayout**（`@/layouts/MainLayout.vue`），布局内已包含底部 Tabbar 与内容区底部占位（含安全区）。
- 底部导航使用 **Wot UI 的 Tabbar**（`layouts/tabbar.vue` 中的 `wd-tabbar`），不采用微信官方的 `custom-tab-bar`；配置与状态在 `stores/tabbar.js`。
- 非主包或未使用 MainLayout 的页面，需自行处理底部安全距离（如 `env(safe-area-inset-bottom)`）。

## 全局反馈组件

- Toast、Loading、Message 统一使用封装的全局反馈组件，**禁止**直接使用 `uni.showToast`、`uni.showLoading`、`uni.showModal` 或 Wot UI 的 `useToast`、`useMessage`。
- 引入路径：`@/composables/feedback/useGlobalLoading`、`@/composables/feedback/useGlobalToast`、`@/composables/feedback/useGlobalMessage`。
- 组件已挂载在虚拟根组件 `App.ku.vue` 中，所有页面共享，无需单独引入。
- 详细 API 见 `src/components/feedback/README.md`。

## 页面跳转

- **跳转统一使用 [uni-mini-router](https://moonofweisheng.github.io/uni-mini-router/guide/installation.html)**，不要直接写 `uni.navigateTo` / `uni.redirectTo` / `uni.reLaunch`。
- 使用 `useRouter()` 获取路由实例，用 `router.push()`、`router.replace()`、`router.pushTab()` 等做跳转；Tab 页用 `router.pushTab()`。路由表由 `uni-read-pages-vite` 从 `pages.json` 注入。

## 参考

- 工程结构与写法可参考 [wot-starter](https://github.com/wot-ui/wot-starter)。
