---
description: 核心技术栈、架构和代码规范
globs: src/stores/**/*, src/composables/**/*, src/router/**/*, src/pages/**/*
alwaysApply: false
---

# 技术栈与架构

本项目为 **uni-app 微信小程序**，技术选型与实现均按小程序场景考虑（包体积、API、生命周期等）。

## 架构概览
- **表现层**: `src/pages` (视图) + `src/components` (UI 组件)
- **状态层**: `src/stores` (Pinia) + `src/composables` (组合式函数)
- **路由层**: `src/router` (uni-mini-router)

## 状态管理 (Pinia)
- 使用 `defineStore` 定义 store，遵循 `use{Name}Store` 命名规范
- 全局反馈相关的 store 放在 `src/composables/feedback/` 下
- 业务 store 放在 `src/stores/` 下

## 路由
- **库**: `uni-mini-router`
- 使用 `useRouter()` 进行 push/replace/back 操作
- Tab 页跳转使用 `router.pushTab()`
- 路由表由 `uni-read-pages-vite` 从 `pages.json` 自动注入
- **禁止**直接使用 `uni.navigateTo` / `uni.redirectTo` / `uni.reLaunch`

## 代码规范
- **语言**: JavaScript（非 TypeScript）
- **ESLint**: 使用 `@uni-helper/eslint-config`，ESLint 同时负责代码检查和格式化，不使用 Prettier
- **缩进**: 4 空格，禁止 Tab
- **Vue 文件块顺序**: `<script>` → `<template>` → `<style>`
- **禁止使用 Vue 2 的 `mixins`**，使用 Vue 3 的 composables（组合式函数）替代
- 组合式函数放在 `src/composables/` 下，遵循 `use{Name}` 命名规范
- **禁止使用 `wx.xxx`**，统一使用 `uni.xxx` API，确保跨平台兼容性

## 工具函数（src/utils）
- **防抖 / 节流 / 只执行一次**：使用 `@/utils/indexUtils` 的 debounce、throttle、once，不手写、不引入 lodash 等。
- **对象与数组**：深拷贝用 deepClone、合并用 mergeObjects、去重用 uniqueArray、按条件查找用 findInArray，均从 `@/utils/objectUtils` 引入。
- **URL 与字符串**：对象转查询串用 objToQuery、查询串解析用 queryToObj、去首尾空格用 trim，从 `@/utils/urlParamsUtils` 引入。
- **本地缓存**：读写删清空缓存统一用 `@/utils/storageUtils` 的 setItem、getItem、removeItem、clearStorage，不直接调 uni.setStorageSync / uni.getStorageSync。

## 校验（src/verification）
- **类型判断**：使用 `@/verification/typeValidate` 的 isString、isNumber、isBoolean、isArray、isObject、isFunction。
- **表单校验**：使用 `@/verification/formValidate` 的 isMobile、isEmail、isAmount、isIDCard、isRequired、isLength、validateForm 等，不手写正则、不引入其他校验库。

## 日期处理
- **统一使用 dayjs**（项目已安装）：`import dayjs from 'dayjs'`
- 时间戳转日期、格式化（如 `dayjs(timestamp).format('YYYY-MM-DD')`）、加减日期等均用 dayjs，不引入 moment、date-fns 等其他日期库。
